## Export Mode

This directory contains the code for my new export mode and extension. 

The mode includes the export button within the toolbar. The code for the toolbar button and logic is contained in the export-extension/src/index.tsx file 